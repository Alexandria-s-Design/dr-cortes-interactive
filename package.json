{
  "name": "dr-cortes-interactive",
  "version": "1.0.0",
  "description": "Interactive Dr. Cortes Timeline with Real-time Avatar Chatbot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"nodemon server.js\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git add -A && git commit -m \"deploy: update site\" && git push origin master"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.20.0",
    "ws": "^8.14.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14",
    "vite": "^7.3.1"
  }
}
